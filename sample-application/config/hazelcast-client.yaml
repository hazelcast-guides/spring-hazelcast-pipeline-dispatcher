hazelcast-client:
  cluster-name: dev
  instance-name: myappserver
  network:
    cluster-members:
      - hz-pipeline:5701
    smart-routing: true
  connection-strategy:
    async-start: true
    reconnect-mode: ASYNC
